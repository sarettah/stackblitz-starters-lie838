export class Team {
  id: number;
  logo: string;
  name: string;

  constructor(id: number, logo: string, name: string) {
    this.id = id;
    this.logo = logo;
    this.name = name;
  }
}
